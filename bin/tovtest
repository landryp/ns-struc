#!/usr/bin/python

import numpy as np
from optparse import OptionParser
from scipy.interpolate import interp1d
from scipy.integrate import ode
from nsstruc.struceqs import *
from nsstruc.constants import *
from nsstruc.tov import *

parser = OptionParser()

opts, args = parser.parse_args()
eospath = str(args[0])
rhoc = float(args[1])
params = str(args[3])

param = params.split(',')

print tov(eospath,rhoc,param)
